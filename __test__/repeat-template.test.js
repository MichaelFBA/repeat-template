import * as RepeatTemplate from '../src/repeat-template.js';

describe('RepeatTemplate is created', function() {
    it('check instance', function() {
        console.log(RepeatTemplate);
        expect(RepeatTemplate).toBeDefined();
    });
});
